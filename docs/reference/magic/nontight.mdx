---
sidebar_position: 4
description: "Convert a single tight list, enum, or terms element to non-tight (with spacing between items)."
---

# nontight

<Signature>
  <code><SignatureName>nontight</SignatureName>(<SignatureParam>[lst](#lst)</SignatureParam>)<SourceLink href="https://github.com/touying-typ/touying/blob/main/src/magic.typ#L112" /></code>
</Signature>

Convert a single tight list, enum, or terms element to non-tight (with spacing between items). For use in show rules.

Usage: `#show list: nontight(list)`

<Parameters>

#### <ParamName>lst</ParamName> : `content` {#lst}

<Param>

A list, enum, or terms element to make non-tight.

</Param>

</Parameters>

**Returns:** `content`
