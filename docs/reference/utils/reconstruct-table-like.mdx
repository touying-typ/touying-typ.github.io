---
sidebar_position: 7
description: "Reconstruct a table-like content with new children."
---

# reconstruct-table-like

<Signature>
  <code><SignatureName>reconstruct-table-like</SignatureName>(<SignatureParam>[named: true](#named), </SignatureParam><SignatureParam>[labeled: true](#labeled), </SignatureParam><SignatureParam>[it](#it), </SignatureParam><SignatureParam>[new-children](#new-children)</SignatureParam>)<SourceLink href="https://github.com/touying-typ/touying/blob/main/src/utils.typ#L193" /></code>
</Signature>

Reconstruct a table-like content with new children.

<Parameters>

#### <ParamName>named</ParamName> : `bool` <Default>`true`</Default> {#named}

<Param>

Whether to pass fields as named arguments. Default is `true`.

</Param>

#### <ParamName>labeled</ParamName> : `bool` <Default>`true`</Default> {#labeled}

<Param>

Whether to preserve the label of the content. Default is `true`.

</Param>

#### <ParamName>it</ParamName> : `content` {#it}

<Param>

The content to reconstruct.

</Param>

#### <ParamName>new-children</ParamName> : `array` {#new-children}

<Param>

The new children to replace the old children with.

</Param>

</Parameters>

**Returns:** `content`
