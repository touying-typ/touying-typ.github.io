---
sidebar_position: 8
description: "Determine if a content is a sequence (i."
---

# is-sequence

<Signature>
  <code><SignatureName>is-sequence</SignatureName>(<SignatureParam>[it](#it)</SignatureParam>)<SourceLink href="https://github.com/touying-typ/touying/blob/main/src/utils.typ#L213" /></code>
</Signature>

Determine if a content is a sequence (i.e. created by concatenating content with `+` or implicit adjacency).

Example: `is-sequence([a])` returns `true`

<Parameters>

#### <ParamName>it</ParamName> : `content` {#it}

<Param>

The content to check.

</Param>

</Parameters>

**Returns:** `bool`
