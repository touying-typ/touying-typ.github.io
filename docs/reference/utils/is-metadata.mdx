---
sidebar_position: 11
description: "Determine if a content is a metadata(...) element."
---

# is-metadata

<Signature>
  <code><SignatureName>is-metadata</SignatureName>(<SignatureParam>[it](#it)</SignatureParam>)<SourceLink href="https://github.com/touying-typ/touying/blob/main/src/utils.typ#L251" /></code>
</Signature>

Determine if a content is a `metadata(...)` element.

Example: `is-metadata(metadata((a: 1)))` returns `true`

<Parameters>

#### <ParamName>it</ParamName> : `content` {#it}

<Param>

The content to check.

</Param>

</Parameters>

**Returns:** `bool`
